header.left-navigation.header(role="banner")
  .left-navigation-wrapper
    nav(role="navigation")
      ul#js-navigation-menu.left-navigation-menu
        each target in ['about', 'projects', 'cv', 'skills']
          li.nav-link
            a(href="#" + "#{target.toLowerCase()}")= target
